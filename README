
#Start the application,the config by default installed in the /etc/ directory 
udpbridge -c /etc/udpbridge.conf

the configuration
49988:this is UDP port
raw:to enable the UDP port transfer all I/O as-is,
600:wait time out
/dev/ttyHSU0:open the device
921600 NONE 1STOPBIT 8DATABITS XONXOFF LOCAL -RTSCTS:A serial port configuration

